--- maze.c.orig 2023-01-01 00:00:00.000000000 +0000
+++ maze.c 2023-03-01 00:00:00.000000000 +0000
@@ -15,10 +15,10 @@
         for (int j = 0; j < size * 2 + 1; ++j) {
             maze[i][j] = wall;
         }
     }

-    // Create paths
+    // Generate paths
     for (int i = 1; i < size; i += 2) {
         for (int j = 1; j < size * 2; j += 2) {
             maze[i][j] = pass;
         }
     }
